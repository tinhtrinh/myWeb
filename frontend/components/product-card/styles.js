import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    root: {
      maxWidth: 280,
      marginBottom: 20,
    },
    media: {
      height: 140,
      width: 280
    },
    alignCenter: {
      margin: 'auto'
    }
  });

export default useStyles;